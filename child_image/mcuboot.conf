# =========================
# MCUboot as USB DFU device
# =========================

# Bootloader runs in supervisor mode (no userspace)
# All threads run in supervisor mode, but USB device stack still needs syscall implementations
# With CONFIG_USERSPACE=n and CONFIG_ENABLE_SYSCALLS=y, syscalls directly call kernel functions
CONFIG_USERSPACE=n
CONFIG_ENABLE_SYSCALLS=y

# Use minimal libc for bootloader (smaller footprint)
CONFIG_MINIMAL_LIBC=y

# Required kernel primitives for Zephyr USB device stack paths you hit
CONFIG_MULTITHREADING=y

CONFIG_SYSTEM_WORKQUEUE=y
CONFIG_WORK_QUEUE=y
CONFIG_SYSTEM_WORKQUEUE_STACK_SIZE=1024
CONFIG_THREAD_NAME=y

CONFIG_QUEUE=y
CONFIG_SEMAPHORE=y
CONFIG_MUTEX=y
CONFIG_POLL=y

# Ensure work queue implementation is available
# USB device stack requires work queue support
CONFIG_WORK_QUEUE_SUBMIT_FROM_ISR=n


# MCUboot USB DFU support (serial recovery over USB)
# This should automatically enable USB device stack
CONFIG_BOOT_SERIAL_CDC_ACM=y

# USB device stack + Nordic USB device controller (required by BOOT_SERIAL_CDC_ACM)
CONFIG_USB=y
CONFIG_USB_DEVICE_STACK=y
CONFIG_USB_NRFX=y
CONFIG_USB_WORKQUEUE_STACK_SIZE=1024
CONFIG_USB_CDC_ACM=y

# MCUboot partition size is defined in pm_static_*.yml file
# The partition size should be at least 0x10000 (64KB) to accommodate USB stack

# Main thread and heap configuration
CONFIG_MAIN_STACK_SIZE=2048
CONFIG_HEAP_MEM_POOL_SIZE=1024
CONFIG_ISR_STACK_SIZE=2048

# Assertions for debugging (can be disabled to save space)
CONFIG_ASSERT=y
CONFIG_ASSERT_LEVEL=2

# Optional debug (can disable later to save space)
CONFIG_LOG=y
CONFIG_CONSOLE=y
CONFIG_UART_CONSOLE=n
